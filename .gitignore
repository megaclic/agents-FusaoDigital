# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# runtime-generated storage (quote PDFs, branding logo/favicon uploads)
/data

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

tmp/*
!tmp/.keep

/generated/prisma

# Wrangler local state
.wrangler

.claude/scheduled_tasks.lock
.claude/worktrees/

# Playwright MCP local output (console logs, traces, screenshots)
.playwright-mcp/

# Python bytecode cache (the onboarding skill ships .py helpers)
__pycache__/
*.pyc

# Local review-loop adjudications (findings reviewed and rejected, with the reason).
# Read from the repo root by the review tooling; kept out of version control.
.codex-review-waived
